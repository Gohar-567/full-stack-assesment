# Test suite — one file per source module mirrors the services/ layout.
#
#   tests/test_hos_engine.py     ← FMCSA HOS compliance (6 targeted cases)
#   tests/test_routing.py        ← (future) routing / geocoding unit tests
#   tests/test_log_generator.py  ← (future) ELD image generation tests
#   tests/test_views.py          ← (future) API integration tests
